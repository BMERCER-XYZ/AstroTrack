{
  "name": "astrotrack",
  "version": "0.1.0",
  "private": true,
  "description": "Static top-down map of planetary positions using NASA JPL Horizons API data",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate_data.js",
    "serve": "python3 -m http.server 8080",
    "preview": "open http://localhost:8080 && python3 -m http.server 8080"
  },
  "devDependencies": {
    "node-fetch": "^3.3.2"
  }
}
